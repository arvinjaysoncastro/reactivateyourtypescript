# 7.4 Print Result - Summary

## Lesson Identity

- Lesson: 7.4 Print Result
- Base exercise file: [7.4-print-result.ts](7.4-print-result.ts)
- Lesson note: [7.4-print-result.md](7.4-print-result.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `7.4-print-result-v1.ts`
- `7.4-print-result-v2.ts`
- `7.4-print-result-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: GRADED

## Attempts

- v1: 82/100
- v2: Pending
- v3: Pending

## Final Grade

- Rate: 82/100
- Strongest Attempt: v1

## Recurring Mistakes

- Concept Error: The processor returns a deterministic object correctly, but the final mapping stops at names instead of the requested `Name (age)` display strings.
- Execution Error: No major execution bug remains; the function returns `{ count, items }` and does not mutate the input array.
- Precision Error: The final task asks for adults mapped to `Name (age)`, but the implementation filters active adults and returns only names.
- Clarity Error: The written explanations are strong overall, though the final implementation does not fully match the narrative about the final result shape.

## Mastery Notes

- final correct understanding: A processor should return a deterministic result object so the final pipeline output can be tested and inspected easily.
- perfect explanation: Filter the target users, map them into the required display shape, and return `{ count, items }` as the source of truth instead of relying on console output.

## Next Action

- For v2, fix only these gaps: map items to `Name (age)` strings, align the filter rule exactly with the task wording, and keep returning `{ count, items }` without mutating users.
