# 5.2 Inner Function Memory - Summary

## Lesson Identity

- Lesson: 5.2 Inner Function Memory
- Base exercise file: [5.2-inner-function-memory.ts](5.2-inner-function-memory.ts)
- Lesson note: [5.2-inner-function-memory.md](5.2-inner-function-memory.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `5.2-inner-function-memory-v1.ts`
- `5.2-inner-function-memory-v2.ts`
- `5.2-inner-function-memory-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: ONGOING

## Attempts

- v1: Graded — 86/100
- v2: Pending
- v3: Pending

## Final Grade

- Rate: 86/100
- Strongest Attempt: v1

## Recurring Mistakes

- Concept Error: incorrect self-check answers (confused whether factory calls produce independent state and whether private variables are inaccessible).
- Execution Error: none that break execution; implementations run as expected.
- Precision Error: typos and small wording issues (e.g., "cerated", "Alhtough").
- Clarity Error: some explanations could be more concise and precise (explain sharing vs. independent closures clearly).

## Mastery Notes

- final correct understanding: closures capture and retain the lexical environment; each factory call normally creates an independent closure instance unless you intentionally reuse shared state.
- perfect explanation: "Each factory call creates a fresh closure; inner functions retain access to that closure's variables so they persist across calls."

## Next Action

- Fix the self-check lines: state that each factory call creates independent state (yes) and that private variables are inaccessible from outside (yes). Correct typos, run the file to verify, then resubmit as `v2` or update `v1` for regrading.
