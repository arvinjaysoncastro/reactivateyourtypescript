# 3 Objects - Summary

## Lesson Identity

- Lesson: 3 Objects
- Base exercise file: [3-objects.ts](3-objects.ts)
- Lesson note: [3-objects.md](3-objects.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `3-objects-v1.ts`
- `3-objects-v2.ts`
- `3-objects-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: ONGOING

## Attempts

- v1: 88/100
- v2: Pending
- v3: Pending

## Final Grade

- Rate: 88/100
- Strongest Attempt: v1

## Subtopic Signal

- 3.1 Object Literals: Understood; object creation, property updates, and grouped data use are correct.
- 3.2 Property Access Dot vs Bracket: Mostly understood; the code is correct, but the required short explanation for why bracket notation was needed is missing.
- 3.3 Nested Objects: Understood; nested structure, deep access, and nested mutation are correct.
- 3.4 Object Mutation: Understood; shared-reference mutation is demonstrated correctly and explained well enough.

## Recurring Mistakes

- Concept Error: Core understanding is mostly correct, but a few explanations are too loose, such as saying an object literal "defines the shape" instead of saying it creates an object with related key-value pairs.
- Execution Error: The work was submitted in the base file instead of a versioned attempt file, and Part 3 missed the required short comment explaining why bracket notation was needed.
- Precision Error: Some wording is not exact enough, such as describing mutation as something that happens only when the object uses `const`, or calling dot notation the "actual path" instead of fixed property access.
- Clarity Error: Minor wording and grammar issues remain, including awkward phrasing and a typo like `orginal`.

## Mastery Notes

- final correct understanding: Objects group related data into one value, dot notation reads fixed keys, bracket notation reads dynamic keys, nested objects require full paths, and mutation changes the original shared object in place.
- perfect explanation: An object literal creates structured data with named properties. Dot notation reads fixed keys, bracket notation reads dynamic keys, nested objects require full paths, and mutation updates the same object directly.

## Next Action

- Create `3-objects-v2.ts` and fix only these items: move the work into the versioned file, add the missing bracket-notation explanation in Part 3, and tighten the wording in the written answers so it uses exact terms like key-value pairs, fixed property access, and mutation of the same object.
