# 5.1 Function Scope - Summary

## Lesson Identity

- Lesson: 5.1 Function Scope
- Base exercise file: [5.1-function-scope.ts](5.1-function-scope.ts)
- Lesson note: [5.1-function-scope.md](5.1-function-scope.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `5.1-function-scope-v1.ts`
- `5.1-function-scope-v2.ts`
- `5.1-function-scope-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: ONGOING

## Attempts

- v1: Graded — 85/100
- v2: Pending
- v3: Pending

## Final Grade

- Rate: 85/100
- Strongest Attempt: v1

## Recurring Mistakes

- Concept Error: minor imprecision describing closures (missing article/clarity).
- Execution Error: syntax errors in `v1` (stray characters after `console.log` calls for `makeAdder`).
- Precision Error: small typos that reduce readability (`hae`, missing words).
- Clarity Error: explanations are correct but could be more precise and concise.

## Mastery Notes

- final correct understanding: student shows correct practical understanding — closures capture and retain outer variables; factory functions create per-instance private state.
- perfect explanation: "A closure is an inner function that retains access to variables from its outer function even after the outer returns."

## Next Action

- Fix the syntax errors (remove stray digits after `console.log`), run the file to verify it executes, then resubmit as `v1` or create `v2` for regrading.
