# 4.1 Array Basics - Summary

## Lesson Identity

- Lesson: 4.1 Array Basics
- Base exercise file: [4.1-array-basics.ts](4.1-array-basics.ts)
- Lesson note: [4.1-array-basics.md](4.1-array-basics.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `4.1-array-basics-v1.ts`
- `4.1-array-basics-v2.ts`
- `4.1-array-basics-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: ONGOING

## Attempts

- v1: Created (focused fixes) — file: [4.1-array-basics-v1.ts](4.1-array-basics-v1.ts)
- v2: Graded 86/100 — file: [4.1-array-basics-v2.ts](4.1-array-basics-v2.ts)
- v3: Graded 72/100 — file: [4.1-array-basics-v3.ts](4.1-array-basics-v3.ts)

## Final Grade

- Rate: 86/100
- Strongest Attempt: v2

## Recurring Mistakes

- Concept Error: initially confused about `map` vs mutation (fixed in v2; v3 contains a correct `map`).
- Execution Error: `topTwo` implementation returns the wrong result (uses `splice(0,1)` on a copy — returns one item instead of two). This is a major correctness issue.
- Execution/Intent Mismatch: PART 3 verification used `splice` on a copied array (works) but the intention was to show a non-mutating approach; prefer `slice` or log the final mutated array explicitly.
- Precision Error: short explanatory sentence has a small typo; polish for clarity.
- Process Error: v3 included Drill 1 content; v3 should contain only the minimal corrected lines in the ANSWER blocks.

## Mastery Notes

- final correct understanding: Core idea is understood; use `map` to return transformed values and prefer `slice`/spread for non-mutating changes.
- perfect explanation: `map` returns a new array of callback results; `forEach` runs side effects and returns `undefined`.

## Next Action

- For v3 resubmission (minimal-only): open `4.1-array-basics-v3.ts` and paste only the corrected lines into the three ANSWER blocks:
	- PART 3 (verification): `const finalStrings = [...strings, 'fourth'].slice(1); console.log(finalStrings);`
	- PART 4 (map): `const objectArray: {id:number, score:number}[] = [{id:1, score:2}]; const increasedArray = objectArray.map(item => ({ id: item.id, score: item.score + 10 })); console.log(increasedArray);`
	- PART 6 (topTwo): `function topTwo(names: string[]): string[] { return names.slice(0,2); }`
	- Save and tell me "grade v3" to regrade.
