# 6.3 Chaining - Summary

## Lesson Identity

- Lesson: 6.3 Chaining
- Base exercise file: [6.3-chaining.ts](6.3-chaining.ts)
- Lesson note: [6.3-chaining.md](6.3-chaining.md)

## Workflow Rule

The base file stays unchanged.

Version files will be created manually by the learner, such as:

- `6.3-chaining-v1.ts`
- `6.3-chaining-v2.ts`
- `6.3-chaining-v3.ts`

Assessment happens on the versioned files.
Later attempts should only fix weak spots instead of repeating the full lesson.

## Status

- Status: GRADED

## Attempts

- v1: 84/100
- v2: Pending
- v3: Pending

## Final Grade

- Rate: 84/100
- Strongest Attempt: v1

## Recurring Mistakes

- Concept Error: The final task is solved sequentially, but it switches from chaining to async/await, so it does not fully demonstrate the lesson's chaining pattern.
- Execution Error: Drill 1 uses post-increment in both then callbacks, so it returns the old value instead of incrementing twice.
- Precision Error: Drill 3 uses Promise<Number> instead of Promise<number>, and the final task is named chainAndSum2 instead of the requested chainAndSum.
- Clarity Error: Part 1 is strong overall, but the wording has a small typo and could be tightened to say the chain waits because the returned promise is linked into the next step.

## Mastery Notes

- final correct understanding: Promise chaining preserves sequence because each then receives the previous result, and returning a promise makes the chain wait for that async step before continuing.
- perfect explanation: Return the next value or promise from each then; if you return a promise, the chain pauses until it settles and then passes that result onward.

## Next Action

- For v2, fix only these gaps: change Drill 1 to use n + 1 instead of post-increment, rename the final function to chainAndSum with Promise<number>, and if you want full lesson precision, solve the final task with chaining rather than async/await.
